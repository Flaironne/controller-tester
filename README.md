# controller-tester

Small Vite + React app with a lobby scene and live control highlighting to verify that a keyboard/gamepad controller is responding.

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```
